import { HomeSection } from "../components/views";
import { MainLayout } from "../layouts";

const Home = () => {
  return (
    <MainLayout>
      <HomeSection />
    </MainLayout>
  );
};

export default Home;