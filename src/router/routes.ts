export enum publicRoutes {
  HOME = '/',
  LOGIN = '/login',
  CONTACT = '/contact',
  NOT_FOUND = '*'
}

export enum privateRoutes {
  SEARCH = '/search',
  RECIPE_BY_ID = '/recipe',
  FAVORITES = '/favorites',
}